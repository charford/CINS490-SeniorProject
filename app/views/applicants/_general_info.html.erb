    <h3>Applicant Name</h3>
    <p>
      <%= link_to User.find(@applicant.user_id), User.find(@applicant.user_id) %>
    </p>
    <h3>Position</h3>
    <p>
      <%= link_to Job.find(@applicant.job_id).position, Job.find(@applicant.job_id) %>
    </p>