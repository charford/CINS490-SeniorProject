<p id="notice"><%= notice %></p>
<div class="full_width_content">
  <h1>Administration</h1>
  <%= render 'pages/admin_tabs' %>
  <div class="fwc_body">
    <h3>Evaluators</h3>
    <table>
      <thead>
        <tr>
          <th>User</th>
          <th></th>
          <th>Created</th>
          <th></th>
          <th></th>
          <th></th>
        </tr>
      </thead>
      <tbody>
        <% @evaluators.each do |evaluator| %>
          <tr>
            <td><%= User.find(evaluator.user_id) %></td>
            <td></td>
            <td><%= evaluator.created_at.strftime("%m/%d/%Y") %></td>
            <td></td>
            <td></td>
            <td style='text-align: right'><%= link_to 'Destroy', evaluator, confirm: 'Are you sure?', method: :delete %></td>
          </tr>
        <% end %>
      </tbody>   
    </table> 
    <h3>Add Evaluator</h3>
    <%= render 'evaluators/form' %>
  </div>
</div>