<%= form_for(@additional_fact) do |f| %>
  <% if @additional_fact.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@additional_fact.errors.count, "error") %> prohibited this additional_fact from being saved:</h2>

      <ul>
      <% @additional_fact.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :evaluator_id %><br />
    <%= f.number_field :evaluator_id %>
  </div>
  <div class="field">
    <%= f.label :applicant_id %><br />
    <%= f.number_field :applicant_id %>
  </div>
  <div class="field">
    <%= f.label :fact %><br />
    <%= f.text_field :fact %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
