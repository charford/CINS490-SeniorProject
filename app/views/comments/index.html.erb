<h1>Listing comments</h1>

<table>
  <tr>
    <th>Applicant</th>
    <th>User</th>
    <th>Body</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @comments.each do |comment| %>
  <tr>
    <td><%= comment.applicant_id %></td>
    <td><%= comment.user_id %></td>
    <td><%= comment.body %></td>
    <td><%= link_to 'Show', job_applicant_comment_path(@job,@applicant,comment) %></td>
    <td><%= link_to 'Edit', edit_job_applicant_comment_path(@job,@applicant,comment) %></td>
    <td><%= link_to 'Destroy', job_applicant_comment_path(@job,@applicant,comment), confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New comment', new_job_jobapp_applicant_comment_path(@job,@job.jobapp,@applicant) %>
