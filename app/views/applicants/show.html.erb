<p id="notice"><%= notice %></p>
<div class="full_width_content">
  <%= render 'pages/tabs' %>
    <div class="fwc_body">
      <div class="applicant_left">
      <%= render 'general_info.html.erb' %>
      <%= render 'average_rating' if has_rated_applicant?(@applicant) %>
    </div>
    <div class="applicant_right">
      <%= render 'survey_responses' %>
      <%= render 'references' %>
      <%= render 'ratings/rate_applicant' if !has_rated_applicant?(@applicant) %>
      <%= render 'ratings' if has_rated_applicant?(@applicant) %>      
    </div>
  </div>
</div>